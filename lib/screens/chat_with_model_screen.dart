import 'package:flutter/material.dart';

class ChatWithModelScreen extends StatelessWidget {
  const ChatWithModelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Wait...."),);
  }
}